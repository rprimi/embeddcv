% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relabeling.R
\name{prepare_relabeling_panel_c}
\alias{prepare_relabeling_panel_c}
\title{Prepare panel C circular relabeling data}
\usage{
prepare_relabeling_panel_c(
  labels_scored,
  scale_label_cross,
  label_by_scale,
  max_constructs = 100,
  solution = c("parsimonious", "optimal"),
  solution_type = NULL,
  labels = NULL,
  instrument = NULL,
  palette = "wulff_mata"
)
}
\arguments{
\item{labels_scored}{Data frame returned by \code{score_relabeling_solutions()}.
It must contain \code{type}, \code{k}, \code{fallacy_sum}, and a list-column
\code{label_vectors}, where each list element is a named scale-to-label vector.}

\item{scale_label_cross}{Numeric scale-by-label similarity matrix. Rows are
scale ids; columns are candidate label ids. It must include the scale ids
and assigned label ids from the selected \code{label_vectors} entry.}

\item{label_by_scale}{Named character vector mapping each scale id to its
original label id. Names must be scale ids and values must be column names
in \code{scale_label_cross}.}

\item{max_constructs}{Integer or numeric scalar. Maximum cluster count for
parsimonious solutions. The comparison is strict (\code{k < max_constructs}).}

\item{solution}{Character scalar, either \code{"parsimonious"} or \code{"optimal"}.
\code{"parsimonious"} selects the lowest-fallacy solution with
\eqn{k <} \code{max_constructs}; \code{"optimal"} selects the lowest-fallacy solution
without that restriction.}

\item{solution_type}{Optional character scalar naming a clustering family to
select within, such as \code{"hclust_ward"}, \code{"kmeans"}, or \code{"mclust"}. If
\code{NULL}, selection is global across candidate families.}

\item{labels}{Optional named character vector mapping scale/label ids to
display text (used for both the outer scale labels and the inner cluster
labels). Ids absent from \code{labels} keep their own value. If \code{NULL} (default),
ids are shown as-is.}

\item{instrument}{Optional named character vector mapping scale ids to an
instrument/inventory name. When supplied, outer scale labels become
\code{"label (instrument)"}; when \code{NULL} (default), only the label is shown.}

\item{palette}{Character scalar. Palette used for label groups. Options are
\code{"wulff_mata"} (default), \code{"cividis"}, \code{"viridis"}, \code{"magma"},
\code{"plasma"}, \code{"inferno"}, \code{"okabe_ito"}, and \code{"blue_gold"}.}
}
\value{
A list consumed by \code{plot_relabeling_panel_c()}, including the final
solution row, scale-to-label assignments, label ranks, circle coordinates,
and color mapping.
}
\description{
Builds the circular assignment layout used in panel C of the relabeling
figure. The function chooses a relabeling solution, extracts its assigned
labels, ranks those labels for each scale, and computes the inner/outer
circle coordinates used by \code{plot_relabeling_panel_c()}. This prepares the
circular relabeling panel in the style of the relabeling analyses reported by
Wulff & Mata (2025).
}
\details{
Let \eqn{F_r = J_r + A_r} be the total fallacy count for candidate solution
\eqn{r}, where \eqn{J_r} is the number of jingle fallacies and \eqn{A_r} is
the number of jangle fallacies returned by \code{score_label_solution()}.
With \code{solution = "parsimonious"}, the selected row is

\deqn{
r^* = \operatorname*{argmin}_{r: k_r < K} (F_r, k_r),
}

where \eqn{K} is \code{max_constructs} and ties are broken by smaller
\eqn{k_r}. With \code{solution = "optimal"}, the same rule is applied without the
\eqn{k_r < K} restriction:

\deqn{
r^* = \operatorname*{argmin}_{r} (F_r, k_r).
}

If \code{solution_type} is supplied, this optimization is restricted to that
clustering family; otherwise it is global across all non-original,
non-Louvain candidate families.

For each scale \eqn{i} and assigned label \eqn{\ell}, the label rank is
computed from the row of \code{scale_label_cross}:

\deqn{\textrm{rank}_{i\ell} = 1 + \#\{m: M_{im} > M_{i\ell}\}.}

A link is marked as \code{covered} when the assigned label is among the five most
similar labels for that scale (\code{rank <= 5}). In the plot, covered links are
drawn as solid lines and uncovered links as dashed/dotted lines, paralleling
the distinction in Wulff & Mata (2025) between assignments with optimal
top-five similarity and assignments with suboptimal similarity.

Scales are placed around the outer circle in sorted assignment order. Labels
are placed on the inner circle at the circular mean of the angles of their
assigned scales. Larger label points indicate that more scales were assigned
to that label. The circular-coordinate and color-blending helpers use local
adaptations of plotting utilities from the \code{memnet} package (Wulff, 2019):
\code{embeddcv_circle_raw()} follows the geometry of the internal \code{circle_raw()}
helper, and \code{mix_col()} follows the RGB interpolation behavior of
\code{memnet::cmix()}.
}
\references{
Wulff, D. U., & Mata, R. (2025). Semantic embeddings reveal and address
taxonomic incommensurability in psychological measurement. Nature Human
Behaviour, 9(5), 944-954.
https://doi.org/10.1038/s41562-024-02089-y

Wulff, D. U. (2019). memnet: Network Tools for Memory Research. R package.
https://www.rdocumentation.org/packages/memnet/versions/0.1.0
}
