% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation_figures.R
\name{plot_structural_fidelity}
\alias{plot_structural_fidelity}
\title{Plot structural fidelity scores}
\usage{
plot_structural_fidelity(
  fidelity_tbl,
  output_file = NULL,
  width = 9,
  height = 4.8,
  dpi = 600,
  palette = "wulff_mata",
  show_legend = FALSE,
  show_scale_labels = FALSE,
  scale_label_fun = identity,
  scale_label_size = NULL,
  scale_label_margin = 0.015,
  scale_tick_length = 0.5,
  recovery_line = 2,
  recovery_line_col = "#D8B365",
  recovery_label_col = "#B2182B",
  recovery_label_fill = "white",
  below_recovery_fill = NULL,
  show_below_recovery_stripes = FALSE,
  below_recovery_stripe_col = "grey55",
  plot_margin = NULL,
  y_limits = NULL
)
}
\arguments{
\item{fidelity_tbl}{Data frame returned by \code{structural_fidelity()}. It must
contain \code{model}, \code{scale}, \code{inventory}, and \code{fidelity}.}

\item{output_file}{Optional character path. If supplied, the plot is written
with \code{ggplot2::ggsave()}.}

\item{width, height, dpi}{Numeric scalars passed to \code{ggplot2::ggsave()} when
\code{output_file} is not \code{NULL}.}

\item{palette}{Character scalar. Color palette for model bars. Options are
\code{"wulff_mata"}, \code{"cividis"}, \code{"viridis"}, \code{"magma"}, \code{"plasma"},
\code{"inferno"}, \code{"okabe_ito"}, and \code{"blue_gold"}.}

\item{show_legend}{Logical scalar. If \code{TRUE}, shows a model legend. The
default is \code{FALSE}, which is cleaner when only one model is plotted.}

\item{show_scale_labels}{Logical scalar. If \code{TRUE}, prints scale names close
to the bottom of the bars. This is supported only when \code{fidelity_tbl}
contains one model. If two or more models are present, the function warns
and changes this argument to \code{FALSE} to avoid implying model-specific scale
ranks.}

\item{scale_label_fun}{Function used to convert scale ids into displayed
axis labels when \code{show_scale_labels = TRUE}. The default keeps ids as-is.}

\item{scale_label_size}{Optional numeric text size for scale labels. If
\code{NULL}, the size is chosen from \code{width} and the number of scales so the
rotated labels are narrower than the bar spacing.}

\item{scale_label_margin}{Numeric scalar. Vertical gap, in data units,
between the x axis and the rotated scale labels when \code{show_scale_labels = TRUE}. Smaller values move labels closer to the bars.}

\item{scale_tick_length}{Numeric scalar, in points. Length of x-axis tick
marks when scale labels are shown.}

\item{recovery_line}{Numeric scalar. Horizontal criterion line. The default
is \code{2}, matching the full-recovery threshold in Wulff & Mata (2025).}

\item{recovery_line_col}{Character scalar. Color of the recovery criterion
line.}

\item{recovery_label_col}{Character scalar. Color of the recovery text.}

\item{recovery_label_fill}{Character scalar. Fill color of the recovery text
box.}

\item{below_recovery_fill}{Optional character scalar. If \code{NULL} (default),
bars below \code{recovery_line} keep the same model color as the remaining
bars. Supply a color to override their fill.}

\item{show_below_recovery_stripes}{Logical scalar. If \code{TRUE}, draws
horizontal stripes over bars below \code{recovery_line}. The default is \code{FALSE},
so bars below the recovery criterion keep the same visual treatment as the
other bars while the dashed recovery line remains visible.}

\item{below_recovery_stripe_col}{Character scalar. Color of horizontal
stripes drawn over bars below \code{recovery_line}. Set to \code{NA} to suppress
stripes even when \code{show_below_recovery_stripes = TRUE}.}

\item{plot_margin}{Optional plot margin in points. If \code{NULL}, margins are
chosen automatically from the label and legend settings. Supply one
numeric value for all sides, a numeric vector in top-right-bottom-left
order, or a \code{ggplot2::margin()} object.}

\item{y_limits}{Optional numeric vector of length 2. If \code{NULL}, limits are
chosen from the data and \code{recovery_line}.}
}
\value{
A ggplot object. If \code{output_file} is supplied, the same plot is saved
to disk.
}
\description{
Draws a compact bar plot of structural fidelity scores. Each bar is a scale
under one embedding model. The dashed horizontal line marks the
\eqn{z = 2} full-recovery criterion, and the layout follows the
structural-fidelity analysis used by Wulff & Mata (2025).
}
\details{
When more than one model is present, bars for the same scale are drawn in the
model order with decreasing width. This makes later models visible in front
of earlier models while preserving a shared scale ordering. The common order
is taken from the first model in \code{fidelity_tbl}.

Scale labels are drawn only when the table contains one model. With two or
more models, scales share the same x positions across models; drawing one
label row can be misleading because the order is a common comparison order,
not a model-specific ranking. If \code{show_scale_labels = TRUE} and two or more
models are present, the function issues a warning and sets
\code{show_scale_labels = FALSE}.
}
\references{
Wulff, D. U., & Mata, R. (2025). Semantic embeddings reveal and address
taxonomic incommensurability in psychological measurement. Nature Human
Behaviour, 9(5), 944-954.
https://doi.org/10.1038/s41562-024-02089-y
}
