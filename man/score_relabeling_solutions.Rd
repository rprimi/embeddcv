% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relabeling.R
\name{score_relabeling_solutions}
\alias{score_relabeling_solutions}
\title{Relabel and score many clustering solutions}
\usage{
score_relabeling_solutions(
  clustering_solutions,
  scale_label_cross,
  scale_cos,
  label_cos,
  thresholds
)
}
\arguments{
\item{clustering_solutions}{Named list from
\code{make_relabeling_clustering_solutions()}. Each top-level element is a
clustering family and contains a list of named membership vectors.}

\item{scale_label_cross}{Numeric scale-by-label similarity matrix used for
maximum-weight relabeling. Rows must include all scale ids used in the
clustering membership vectors; columns are candidate label ids.}

\item{scale_cos}{Numeric square scale-scale similarity matrix used for
fallacy scoring. Row and column names must include the scale ids in the
clustering solutions.}

\item{label_cos}{Numeric square label-label similarity matrix used for
fallacy scoring. Row and column names must include candidate label ids from
\code{colnames(scale_label_cross)}.}

\item{thresholds}{Named numeric vector from \code{jingle_jangle_thresholds()} with
\code{scale_low}, \code{label_low}, \code{scale_high}, and \code{label_high}.}
}
\value{
A data frame with one row per valid clustering solution and columns:
\describe{
\item{type}{Clustering family, such as \code{hclust_ward}, \code{kmeans}, or
\code{mclust}.}
\item{k}{Number of unique clusters in the solution.}
\item{good, jangle, jingle, fallacy_sum}{Fallacy score counts.}
\item{label_vectors}{List-column containing the relabeled scale-to-label
vector for that solution.}
}
}
\description{
Applies the full relabeling score pipeline to every candidate solution
generated by \code{make_relabeling_clustering_solutions()}: first each clustering
solution is relabeled with \code{relabel_solution()}, then jingle and jangle
fallacies are counted with \code{score_label_solution()}.
}
\details{
The returned table is the main input for the relabeling figure. For each
candidate taxonomy it stores the algorithm family, the realized number of
clusters \eqn{k}, the number of jingle fallacies, the number of jangle
fallacies, their sum, and the vector of assigned labels.

For each membership vector \eqn{r}, \code{score_clustering_solution()} first calls
\code{relabel_solution()} to obtain a scale-to-label assignment
\eqn{\hat{\ell}_i}. It then evaluates every unordered scale pair
\eqn{i < j}. Let \eqn{S_{ij}} be scale-scale semantic similarity and let
\eqn{L_{\hat{\ell}_i\hat{\ell}_j}} be the semantic similarity between the
labels assigned to those two scales. Given thresholds
\eqn{s_{low}}, \eqn{s_{high}}, \eqn{l_{low}}, and \eqn{l_{high}}, the counts
are:

\deqn{
J_r = \sum_{i < j}
I(S_{ij} < s_{low}, L_{\hat{\ell}_i\hat{\ell}_j} > l_{high})
}

for jingle fallacies and

\deqn{
A_r = \sum_{i < j}
I(S_{ij} > s_{high}, L_{\hat{\ell}_i\hat{\ell}_j} < l_{low})
}

for jangle fallacies. The solution score is

\deqn{F_r = J_r + A_r,}

returned as \code{fallacy_sum}. All other unordered pairs are counted as \code{good}.

Some clustering methods can fail for specific values of \code{k}, especially
k-means and Mclust. \code{make_relabeling_clustering_solutions()} stores these as
\code{NULL}, and this function silently skips them.
}
\references{
Wulff, D. U., & Mata, R. (2025). Semantic embeddings reveal and address
taxonomic incommensurability in psychological measurement. Nature Human
Behaviour, 9(5), 944-954.
https://doi.org/10.1038/s41562-024-02089-y
}
