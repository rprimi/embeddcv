% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jingle_jangle.R
\name{classify_jingle_jangle}
\alias{classify_jingle_jangle}
\title{Classify scale pairs as jingle or jangle fallacies}
\usage{
classify_jingle_jangle(pair_table, thresholds, verbose = TRUE)
}
\arguments{
\item{pair_table}{Data frame or tibble, usually the output of
\code{jingle_jangle_pairs()}. It must contain numeric columns \code{scale_cos} and
\code{label_cos}, plus display columns \code{label_1}, \code{label_2}, \code{instrument_1},
and \code{instrument_2}.}

\item{thresholds}{Named numeric vector, usually from
\code{jingle_jangle_thresholds()}. It must contain \code{scale_low}, \code{label_low},
\code{scale_high}, and \code{label_high}.}

\item{verbose}{Logical scalar. If \code{TRUE}, prints the number of jingle
fallacies, jangle fallacies, and non-fallacy pairs.}
}
\value{
\code{pair_table} with added columns:
\describe{
\item{type}{\code{"Jingle fallacy"}, \code{"Jangle fallacy"}, or \code{"nothing"}.}
\item{cos_diff}{Absolute difference between scale and label similarity.}
\item{label}{Short display label for plots.}
}
}
\description{
Adds fallacy labels to a pair table created by \code{jingle_jangle_pairs()}.
This implements the automated jingle-jangle detection rule described by
Wulff & Mata (2025): jingle fallacies are pairs with similar labels but
dissimilar scale content, and jangle fallacies are pairs with dissimilar
labels but similar scale content.
}
\details{
Each pair is classified using the same rules as \code{score_label_solution()}:

\deqn{\textrm{jingle} \iff \textrm{scale\_cos} < s_{low}
\ \textrm{and}\ \textrm{label\_cos} > l_{high}}

\deqn{\textrm{jangle} \iff \textrm{scale\_cos} > s_{high}
\ \textrm{and}\ \textrm{label\_cos} < l_{low}.}

A jingle fallacy means labels are semantically similar while the scales are
semantically dissimilar. A jangle fallacy means labels are semantically
dissimilar while the scales are semantically similar.
}
\references{
Wulff, D. U., & Mata, R. (2025). Semantic embeddings reveal and address
taxonomic incommensurability in psychological measurement. Nature Human
Behaviour, 9(5), 944-954.
https://doi.org/10.1038/s41562-024-02089-y
}
