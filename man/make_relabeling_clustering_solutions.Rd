% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relabeling.R
\name{make_relabeling_clustering_solutions}
\alias{make_relabeling_clustering_solutions}
\title{Generate clustering solutions for relabeling analyses}
\usage{
make_relabeling_clustering_solutions(
  scale_cos,
  k_seq = seq_len(nrow(scale_cos)),
  original_solution = NULL,
  include_mclust = TRUE,
  include_louvain = TRUE,
  louvain_resolution = seq(0.5, 5, 0.05),
  kmeans_nstart = 1,
  kmeans_iter_max = 50,
  seed = 42
)
}
\arguments{
\item{scale_cos}{Numeric square scale-scale similarity matrix with identical
row and column names in the same order. Rows/columns are scales; values are
usually cosine similarities, optionally rescaled with
\code{rescale_similarity()}. The row names become the scale ids used throughout
the returned clustering solutions.}

\item{k_seq}{Integer vector with target numbers of clusters to evaluate for
hierarchical clustering, k-means, and Mclust. Values should be between 1
and \code{nrow(scale_cos)}.}

\item{original_solution}{Optional named vector with an original taxonomy or
cluster assignment. Names must be scale ids matching \code{rownames(scale_cos)};
values are cluster ids/construct ids. If supplied, it is stored as
\code{original_b5} and can be used as a reference solution.}

\item{include_mclust}{Logical scalar. If \code{TRUE}, fits \code{mclust::Mclust()} for
each \code{k} when the \code{mclust} package is installed; otherwise returns empty
Mclust slots.}

\item{include_louvain}{Logical scalar. If \code{TRUE}, creates Louvain
graph-community solutions with \code{igraph::cluster_louvain()}.}

\item{louvain_resolution}{Numeric vector of Louvain resolution values. Each
value is passed to \code{igraph::cluster_louvain(resolution = ...)}; larger
values usually produce more communities.}

\item{kmeans_nstart}{Integer scalar. Number of random starts passed to
\code{stats::kmeans()}. Increase this for more stable k-means solutions.}

\item{kmeans_iter_max}{Integer scalar. Maximum number of iterations passed to
\code{stats::kmeans()}.}

\item{seed}{Integer scalar. Random seed used before k-means and Louvain
clustering.}
}
\value{
A named list. Each top-level element is a clustering family
(\code{hclust_complete}, \code{hclust_single}, \code{hclust_ward}, \code{kmeans}, \code{mclust},
\code{louvain}, and optionally \code{original_b5}). Each family contains a list of
named membership vectors, one vector per requested solution. Vector names
are scale ids and vector values are cluster ids.
}
\description{
Builds the candidate taxonomies that are later relabeled and scored. The
function generates the solution families used by Wulff & Mata (2025):
hierarchical complete, single, and Ward clustering; k-means; optional
Gaussian mixture clustering via \code{mclust}; optional Louvain community
detection; and an optional original taxonomy supplied by the user.
}
\details{
The input is a scale-scale semantic similarity matrix \eqn{S}. For
hierarchical clustering, similarities are converted to distances with

\deqn{D_{ij} = 1 - S_{ij}.}

Let \eqn{S} be the supplied scale-scale similarity matrix and
\eqn{D_{ij} = 1 - S_{ij}} be the dissimilarity used for agglomerative
clustering. Three hierarchical linkage rules are evaluated with
\code{stats::hclust(stats::as.dist(D), method = ...)}:
\describe{
\item{\code{hclust_complete}}{complete linkage via \code{stats::hclust(..., method = "complete")}, where the distance between candidate clusters is
the largest pairwise dissimilarity,
\eqn{d(A,B) = \max_{i \in A, j \in B} D_{ij}}.}
\item{\code{hclust_single}}{single linkage via \code{method = "single"}, where the
distance between candidate clusters is the smallest pairwise dissimilarity,
\eqn{d(A,B) = \min_{i \in A, j \in B} D_{ij}}.}
\item{\code{hclust_ward}}{Ward's minimum variance method via
\code{method = "ward.D2"}. This calls R's Ward.D2 implementation directly; no
custom Ward update is implemented inside \code{embeddcv}.}
}

For each value in \code{k_seq}, the hierarchical trees are cut with
\code{stats::cutree(k = k)}. K-means is run directly on the rows of \eqn{S}; that
is, each scale is represented by its similarity profile to all scales, and
\code{stats::kmeans()} minimizes the within-cluster squared Euclidean criterion
\eqn{\sum_i ||s_i - \mu_{c_i}||^2}. If \code{mclust} is available and
\code{include_mclust = TRUE}, \code{mclust::Mclust()} is also run on the rows of
\eqn{S} with \code{G = k}; the returned mixture classification is stored as the
solution for that \eqn{k}.

Louvain clustering uses a min-max transformed similarity matrix as a weighted
undirected graph:

\deqn{A_{ij} = \frac{S_{ij} - \min(S)}{\max(S) - \min(S)}.}

\code{igraph::cluster_louvain()} is then evaluated across
\code{louvain_resolution}. Louvain solutions are useful for sensitivity checks;
by default the selection helpers exclude them from parsimonious/optimal
solution selection so that the main comparison focuses on hierarchical
clustering, k-means, and Mclust.
}
\examples{
set.seed(1)
scale_cos <- matrix(runif(25, .2, .9), 5)
scale_cos <- (scale_cos + t(scale_cos)) / 2
diag(scale_cos) <- 1
rownames(scale_cos) <- colnames(scale_cos) <- paste0("scale_", 1:5)

sols <- make_relabeling_clustering_solutions(
  scale_cos,
  k_seq = 2:4,
  include_mclust = FALSE,
  include_louvain = FALSE
)
names(sols)
sols$hclust_ward[[1]]
}
\references{
Wulff, D. U., & Mata, R. (2025). Semantic embeddings reveal and address
taxonomic incommensurability in psychological measurement. Nature Human
Behaviour, 9(5), 944-954.
https://doi.org/10.1038/s41562-024-02089-y
}
