% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_embeddings_hf_local.R
\name{get_embeddings_hf_local}
\alias{get_embeddings_hf_local}
\title{Generate Text Embeddings using HuggingFace Models Locally}
\usage{
get_embeddings_hf_local(
  text,
  model = "dwulff/mpnet-personality",
  python_env = NULL,
  install_packages = TRUE
)
}
\arguments{
\item{text}{Character vector. One or more texts to be transformed into embeddings.}

\item{model}{Character. The sentence-transformers model to use
(default: \code{'dwulff/mpnet-personality'}).}

\item{python_env}{Character. Path to a Python executable or virtual environment.
If \code{NULL}, uses the default reticulate environment.}

\item{install_packages}{Logical. Whether to automatically install required Python
packages if missing (default: \code{TRUE}).}
}
\value{
A data frame where each row is the embedding vector for the corresponding
input text.
}
\description{
Generates text embeddings using HuggingFace's sentence-transformers library
running locally via Python (reticulate). Useful for models not available via
the Inference API, or for offline use.
}
\details{
Requires the Python packages \code{sentence_transformers} and \code{torch}.
The model is downloaded on first use and cached locally by sentence-transformers.
}
\examples{
\dontrun{
 embeddings <- get_embeddings_hf_local(
   text  = c("I am outgoing", "I worry a lot", "I like art"),
   model = "dwulff/mpnet-personality"
 )
 dim(embeddings)
}

}
