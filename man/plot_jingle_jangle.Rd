% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jingle_jangle.R
\name{plot_jingle_jangle}
\alias{plot_jingle_jangle}
\title{Plot a jingle-jangle line figure}
\usage{
plot_jingle_jangle(
  pair_table,
  thresholds,
  output_file = NULL,
  width = 8.7,
  height = 6.5,
  res = 600,
  n_labels = 60,
  n_background = 10000,
  seed = 42,
  background_col = "grey85",
  background_lwd = 0.07,
  fallacy_lwd = 0.07,
  label_cex = 0.5,
  auto_height = TRUE,
  label_line_height = 0.07,
  palette = "wulff_mata",
  show_count_note = FALSE,
  count_note = NULL,
  count_note_cex = 0.72,
  count_note_col = "grey35",
  verbose = TRUE
)
}
\arguments{
\item{pair_table}{Data frame or tibble returned by
\code{classify_jingle_jangle()}. It must contain numeric \code{label_cos},
\code{scale_cos}, and \code{cos_diff} columns, a character/factor \code{type} column with
values \code{"Jingle fallacy"}, \code{"Jangle fallacy"}, and \code{"nothing"}, and a
character \code{label} column used for side labels.}

\item{thresholds}{Named numeric vector returned by
\code{jingle_jangle_thresholds()}, with \code{scale_low}, \code{label_low},
\code{scale_high}, and \code{label_high}. The values determine the bracket positions
and should match the thresholds used to classify \code{pair_table}.}

\item{output_file}{Optional character path. If supplied, a PNG is written
with \code{grDevices::png()}; if \code{NULL}, the plot is drawn on the active device.}

\item{width, height}{Numeric scalars. PNG size in inches when \code{output_file} is
supplied.}

\item{res}{Numeric scalar. PNG resolution in pixels per inch when
\code{output_file} is supplied.}

\item{n_labels}{Integer scalar. Maximum number of highlighted fallacy labels
sampled and printed per side. If more fallacies exist, all fallacy line
segments are still drawn, but only \code{n_labels} side labels are sampled. Use
\code{Inf} to print all highlighted labels. When \code{output_file} is supplied and
\code{auto_height = TRUE}, the figure height grows with the number of printed
labels to reduce overlap.}

\item{n_background}{Integer scalar. Maximum number of grey background lines
to draw. Lower values make the plot faster and lighter; higher values make
the background density closer to the full pair table.}

\item{seed}{Integer scalar. Random seed for reproducible fallacy label and
background sampling.}

\item{background_col}{Character scalar. Color for non-fallacy background
lines.}

\item{background_lwd}{Numeric scalar. Line width for non-fallacy background
lines.}

\item{fallacy_lwd}{Numeric scalar. Line width for jingle and jangle fallacy
lines.}

\item{label_cex}{Numeric scalar. Constant text size for side labels.}

\item{auto_height}{Logical scalar. If \code{TRUE} and \code{output_file} is supplied,
increases \code{height} when many side labels are printed.}

\item{label_line_height}{Numeric scalar. Approximate inches reserved per
printed side label when \code{auto_height = TRUE}.}

\item{palette}{Character scalar. Color palette for jingle and jangle lines
and labels. Options
are \code{"wulff_mata"} (default), \code{"cividis"}, \code{"viridis"}, \code{"magma"},
\code{"plasma"}, \code{"inferno"}, \code{"okabe_ito"}, and \code{"blue_gold"}.}

\item{show_count_note}{Logical scalar. If \code{TRUE}, adds a footnote below the
figure with the number of jingle fallacies, jangle fallacies, and their
total. The default is \code{FALSE} so package users opt in explicitly.}

\item{count_note}{Optional character scalar. Custom note to draw when
\code{show_count_note = TRUE}. If \code{NULL}, the note is generated from
\code{pair_table} and starts with an asterisk.}

\item{count_note_cex}{Numeric scalar. Text size for the fallacy count note.}

\item{count_note_col}{Character scalar. Text color for the fallacy count
note.}

\item{verbose}{Logical scalar. If \code{TRUE}, prints the number of jingle and
jangle fallacies in \code{pair_table} and the number of side labels drawn.}
}
\value{
Invisibly returns \code{pair_table}.
}
\description{
Draws the line figure used to visualize jingle and jangle fallacies in
Wulff & Mata (2025). Each line is one pair of scales: the left endpoint is
label similarity and the right endpoint is scale similarity. Grey lines are
ordinary pairs that do not meet either fallacy criterion; colored lines are
pairs classified as jingle or jangle fallacies. Sampled fallacy labels are
printed on the left and right sides.
}
\details{
Each pair is drawn as a line connecting label similarity on the left axis to
scale similarity on the right axis. Jingle pairs have high label similarity
but low scale similarity; jangle pairs have low label similarity but high
scale similarity. The thresholds determine the bracket positions and should
usually be the same thresholds used by \code{classify_jingle_jangle()}.

If \code{output_file} is supplied, a PNG device is opened with
\code{grDevices::png(width = width, height = height, res = res, units = "in")}.
When \code{auto_height = TRUE}, the saved PNG height is increased as needed so
sampled side labels can keep a constant text size instead of being compressed
into the original plotting height.
}
\references{
Wulff, D. U., & Mata, R. (2025). Semantic embeddings reveal and address
taxonomic incommensurability in psychological measurement. Nature Human
Behaviour, 9(5), 944-954.
https://doi.org/10.1038/s41562-024-02089-y
}
