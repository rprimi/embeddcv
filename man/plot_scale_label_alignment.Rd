% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation_figures.R
\name{plot_scale_label_alignment}
\alias{plot_scale_label_alignment}
\title{Plot scale-label alignment scores}
\usage{
plot_scale_label_alignment(
  alignment_tbl,
  output_file = NULL,
  width = 6.5,
  height = 4.2,
  dpi = 300,
  palette = "wulff_mata",
  y_limits = NULL,
  point_size = 10.5
)
}
\arguments{
\item{alignment_tbl}{Data frame returned by \code{scale_label_alignment()}. It
must contain \code{definition_set} and \code{alignment}; if it also contains
\code{definition_group}, points are arranged within those x-axis groups.}

\item{output_file}{Optional character path. If supplied, the plot is written
with \code{ggplot2::ggsave()}.}

\item{width, height, dpi}{Numeric scalars passed to \code{ggplot2::ggsave()} when
\code{output_file} is not \code{NULL}.}

\item{palette}{Character scalar. Color palette for points. Options are
\code{"wulff_mata"}, \code{"cividis"}, \code{"viridis"}, \code{"magma"}, \code{"plasma"},
\code{"inferno"}, \code{"okabe_ito"}, and \code{"blue_gold"}.}

\item{y_limits}{Optional numeric vector of length 2. The default covers 0 to
at least 3.5, matching the scale used by Wulff & Mata (2025) when values
are in that range.}

\item{point_size}{Numeric scalar. Size of the square points.}
}
\value{
A ggplot object. If \code{output_file} is supplied, the same plot is saved
to disk.
}
\description{
Plots the mean scale-label alignment score for each supplied label or
definition set. Each point is the average z-score alignment across scales.
The layout follows the scale-label alignment analysis shown by Wulff & Mata
(2025). Guide-line colors are lightened with \code{mix_col()}, a local color
mixer adapted from \code{memnet::cmix()} (Wulff, 2019).
}
\references{
Wulff, D. U., & Mata, R. (2025). Semantic embeddings reveal and address
taxonomic incommensurability in psychological measurement. Nature Human
Behaviour, 9(5), 944-954.
https://doi.org/10.1038/s41562-024-02089-y

Wulff, D. U. (2019). memnet: Network Tools for Memory Research. R package.
https://www.rdocumentation.org/packages/memnet/versions/0.1.0
}
