% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jingle_jangle.R
\name{jingle_jangle_pairs}
\alias{jingle_jangle_pairs}
\title{Build scale-label pair data for jingle-jangle analysis}
\usage{
jingle_jangle_pairs(
  scale_cos,
  label_cos,
  label_by_scale = NULL,
  label_fun = scale_label_from_scale,
  instrument_fun = instrument_from_scale,
  upper_only = TRUE
)
}
\arguments{
\item{scale_cos}{Numeric square scale-scale similarity matrix. Row and column
names must be identical scale ids in the same order. This matrix is often
produced by \code{cosine_matrix(scale_embeddings)} followed by
\code{rescale_similarity()} when clustering should use a 0-1 range.}

\item{label_cos}{Numeric square label-label similarity matrix. Row and column
names must contain all label ids referenced by \code{label_by_scale}. This
matrix is often produced by \code{cosine_matrix(label_embeddings)}.}

\item{label_by_scale}{Optional named character vector mapping scale ids to
label ids. \code{names(label_by_scale)} must include every row name in
\code{scale_cos}; the values must be row/column names in \code{label_cos}. If \code{NULL},
scale ids are assumed to be identical to label ids.}

\item{label_fun}{Function that accepts a character vector of scale ids and
returns display construct labels of the same length. For example, the
default \code{scale_label_from_scale()} turns \code{"pers_inv_social_boldness"} into
\code{"inv_social_boldness"}.}

\item{instrument_fun}{Function that accepts a character vector of scale ids
and returns display instrument names of the same length. The default
\code{instrument_from_scale()} turns \code{"pers_inv_social_boldness"} into
\code{"pers"}.}

\item{upper_only}{Logical scalar. If \code{TRUE}, keep only unique unordered scale
pairs (\code{i < j}). If \code{FALSE}, all ordered pairs are returned.}
}
\value{
Data frame with scale ids, display labels, instrument labels, mapped
label ids, \code{label_cos}, and \code{scale_cos}.
}
\description{
Converts aligned scale-scale and label-label similarity matrices into a long
pair table. Each row corresponds to a pair of scales and contains both the
similarity between the scales and the similarity between the labels attached
to those scales. This is the pair-construction step needed for the automated
jingle-jangle detection approach described by Wulff & Mata (2025).
}
\details{
If \code{label_by_scale} is supplied, it maps each scale id to the corresponding
label id in \code{label_cos}. This is important when item-derived scale ids and
definition/label ids are not identical. For a scale pair \eqn{(i,j)}, the
function returns:

\deqn{\textrm{scale\_cos}_{ij} = S_{ij}}

and

\deqn{\textrm{label\_cos}_{ij} = L_{m(i),m(j)},}

where \eqn{m(i)} is the label id mapped to scale \eqn{i}.
}
\references{
Wulff, D. U., & Mata, R. (2025). Semantic embeddings reveal and address
taxonomic incommensurability in psychological measurement. Nature Human
Behaviour, 9(5), 944-954.
https://doi.org/10.1038/s41562-024-02089-y
}
