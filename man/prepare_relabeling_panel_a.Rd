% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relabeling.R
\name{prepare_relabeling_panel_a}
\alias{prepare_relabeling_panel_a}
\title{Prepare panel A relabeling data}
\usage{
prepare_relabeling_panel_a(
  labels_scored,
  max_constructs = 100,
  solution_type = NULL
)
}
\arguments{
\item{labels_scored}{Data frame returned by \code{score_relabeling_solutions()}.
It must contain at least one candidate clustering family and columns
\code{type}, \code{k}, \code{fallacy_sum}, \code{jangle}, and \code{jingle}. A row with
\code{type == "original_b5"} is optional and, if present, supplies the reference
fallacy line.}

\item{max_constructs}{Integer or numeric scalar. Maximum cluster count for
parsimonious solutions. The comparison is strict (\code{k < max_constructs}).}

\item{solution_type}{Optional character scalar naming the clustering family
to plot, such as \code{"hclust_ward"}, \code{"kmeans"}, or \code{"mclust"}. If \code{NULL},
the family of the global parsimonious solution is used.}
}
\value{
A list consumed by \code{plot_relabeling_panel_a()}, including the
solution table, selected solutions, axis ticks, and plotting ranges.
}
\description{
Extracts the clustering solutions needed for panel A of a relabeling figure
in the style of the analyses reported by Wulff & Mata (2025). Panel A plots
fallacy counts as a function of the number of clusters for one clustering
family.
}
\details{
If \code{solution_type = NULL}, the panel uses the clustering family of the global
parsimonious solution: among non-original and non-Louvain rows with
\eqn{k <} \code{max_constructs}, it selects the row with the smallest
\code{fallacy_sum}, breaking ties by smaller \code{k}, and then plots the full
trajectory for that row's \code{type}. You can set \code{solution_type} explicitly to
draw a particular family, such as \code{"hclust_ward"}, \code{"kmeans"}, or \code{"mclust"}.

It stores the total, jingle, and jangle fallacy curves, the original taxonomy
fallacy reference line (if present), and two selected solutions from the same
clustering family:

\deqn{\textrm{parsimonious} =
\operatorname*{argmin}_{k < \textrm{max\_constructs}}
(\textrm{fallacy\_sum}, k)}

and

\deqn{\textrm{optimal} =
\operatorname*{argmin}(\textrm{fallacy\_sum}, k).}
}
\references{
Wulff, D. U., & Mata, R. (2025). Semantic embeddings reveal and address
taxonomic incommensurability in psychological measurement. Nature Human
Behaviour, 9(5), 944-954.
https://doi.org/10.1038/s41562-024-02089-y
}
