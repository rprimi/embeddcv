% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relabeling.R
\name{prepare_relabeling_panels}
\alias{prepare_relabeling_panels}
\title{Prepare data for a relabeling figure}
\usage{
prepare_relabeling_panels(
  labels_scored,
  scale_label_cross,
  label_by_scale,
  max_constructs = 100,
  solution = c("parsimonious", "optimal"),
  solution_type = NULL,
  label_fun = scale_label_from_scale,
  instrument_fun = instrument_from_scale,
  palette = "wulff_mata",
  verbose = TRUE
)
}
\arguments{
\item{labels_scored}{Data frame returned by \code{score_relabeling_solutions()}.
It must contain the columns used by panels A and B (\code{type}, \code{k},
\code{fallacy_sum}, \code{jangle}, \code{jingle}) and the list-column \code{label_vectors}
needed by panel C.}

\item{scale_label_cross}{Numeric scale-by-label similarity matrix used to
compute panel C label ranks and assignments. Rows are scale ids and
columns are candidate label ids. It is usually
\code{cosim_itens_scales(scale_emb, label_emb)$cosim_matrix}.}

\item{label_by_scale}{Named character vector mapping each scale id to its
original label id. Names must correspond to row names of
\code{scale_label_cross}; values must correspond to columns of
\code{scale_label_cross}.}

\item{max_constructs}{Integer or numeric scalar. Maximum cluster count for
parsimonious solutions; the comparison is strict (\code{k < max_constructs}).}

\item{solution}{Character scalar, either \code{"parsimonious"} or \code{"optimal"}.
\code{"parsimonious"} selects the lowest-fallacy solution with
\eqn{k <} \code{max_constructs}; \code{"optimal"} selects the lowest-fallacy solution
without that restriction.}

\item{solution_type}{Optional character scalar naming a clustering family to
select within, such as \code{"hclust_ward"}, \code{"kmeans"}, or \code{"mclust"}. If
\code{NULL}, selection is global across all candidate clustering families.}

\item{label_fun}{Function that accepts scale/label ids and returns display
labels of the same length. The default \code{scale_label_from_scale()} removes
the instrument prefix, e.g. \code{"inventory_social_boldness"} becomes
\code{"social_boldness"}.}

\item{instrument_fun}{Function that accepts scale ids and returns instrument
labels of the same length. The default \code{instrument_from_scale()} keeps the
prefix before the first underscore, e.g. \code{"inventory_social_boldness"}
becomes \code{"inventory"}.}

\item{palette}{Character scalar. Palette used when preparing panel C colors.
Options are
\code{"wulff_mata"} (default), \code{"cividis"}, \code{"viridis"}, \code{"magma"},
\code{"plasma"}, \code{"inferno"}, \code{"okabe_ito"}, and \code{"blue_gold"}.}

\item{verbose}{Logical scalar. If \code{TRUE}, prints the selected clustering
family and its parsimonious/optimal fallacy counts.}
}
\value{
A list of class \code{embeddcv_relabeling_panels} with elements
\code{panel_a}, \code{panel_b}, and \code{panel_c}. Each element can be passed directly to
its corresponding plotting function.
}
\description{
Separates data preparation from plotting for a three-panel relabeling figure
in the style of the analyses reported by Wulff & Mata (2025). This is useful
when you want to inspect or customize the panel data before drawing, or when
you want to redraw the same relabeling solution with different text settings
or palettes.
}
\details{
The function is a convenience wrapper around:
\describe{
\item{\code{prepare_relabeling_panel_a()}}{extracts the fallacy-count curve for
the clustering family used by the selected solution and identifies that
family's parsimonious/optimal solutions.}
\item{\code{prepare_relabeling_panel_b()}}{selects parsimonious/optimal
solutions for each clustering family.}
\item{\code{prepare_relabeling_panel_c()}}{selects the final relabeling solution
and computes the circular layout of scales, labels, links, ranks, and
colors.}
}

The default selection is the global parsimonious relabeling solution: among
the candidate clustering families, it selects the row with
\eqn{k <} \code{max_constructs} that minimizes \code{fallacy_sum}, breaking ties by the
smaller \code{k}. Set \code{solution = "optimal"} to choose the minimum \code{fallacy_sum}
without the parsimony restriction. Set \code{solution_type} to a clustering family
such as \code{"hclust_ward"}, \code{"kmeans"}, or \code{"mclust"} to select within that
family instead of across all families. Panel A then uses the full
fallacy-count trajectory for the same clustering family selected for panel C.
}
\references{
Wulff, D. U., & Mata, R. (2025). Semantic embeddings reveal and address
taxonomic incommensurability in psychological measurement. Nature Human
Behaviour, 9(5), 944-954.
https://doi.org/10.1038/s41562-024-02089-y
}
