% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jingle_jangle.R
\name{jingle_jangle_threshold_grid}
\alias{jingle_jangle_threshold_grid}
\title{Evaluate jingle-jangle sensitivity over cutoff grids}
\usage{
jingle_jangle_threshold_grid(
  pair_table,
  scale_cos,
  label_cos,
  lows = 1:10,
  highs = seq(50, 250, 20)
)
}
\arguments{
\item{pair_table}{Data frame or tibble, usually the output of
\code{jingle_jangle_pairs()}. It must contain numeric \code{scale_cos} and
\code{label_cos} columns because it is repeatedly passed to
\code{classify_jingle_jangle()}.}

\item{scale_cos}{Numeric scale-scale similarity matrix or vector used to
compute scale cutoffs. Use the same object used to create \code{pair_table}.}

\item{label_cos}{Numeric label-label similarity matrix or vector used to
compute label cutoffs. Use the same object used to create \code{pair_table}.}

\item{lows}{Numeric vector of low-cutoff denominators. Each value must be
greater than 1; for example, \code{5} means the low cutoff is the 80th
percentile.}

\item{highs}{Numeric vector of high-cutoff denominators. Each value must be
greater than 1; for example, \code{100} means the high cutoff is the 99th
percentile.}
}
\value{
Data frame with one row per grid cell and columns for the denominator
values, grid coordinates (\code{x}, \code{y}), jingle/jangle counts, and rounded
cutoff values.
}
\description{
Recomputes jingle and jangle counts over a grid of threshold rules. This is
the data-preparation step behind robustness plots that ask whether the number
of fallacies is stable across stricter or looser semantic cutoff definitions.
}
\details{
For each combination of \code{lows} and \code{highs}, thresholds are computed with
\code{jingle_jangle_thresholds(min_constructs = lows[i], max_constructs = highs[j])}. Thus each low denominator \eqn{a} corresponds to a quantile
\eqn{1 - 1/a}, and each high denominator \eqn{b} corresponds to
\eqn{1 - 1/b}.

The default grid mirrors the sensitivity grid in Wulff & Mata (2025):
\code{lows = 1:10} evaluates low cutoffs from \eqn{q_0} through \eqn{q_{90}},
and \code{highs = seq(50, 250, 20)} evaluates high cutoffs from approximately
\eqn{q_{98}} through \eqn{q_{99.6}}.
}
\references{
Wulff, D. U., & Mata, R. (2025). Semantic embeddings reveal and address
taxonomic incommensurability in psychological measurement. Nature Human
Behaviour, 9(5), 944-954.
https://doi.org/10.1038/s41562-024-02089-y
}
