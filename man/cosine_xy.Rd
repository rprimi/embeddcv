% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cosine_xy.R
\name{cosine_xy}
\alias{cosine_xy}
\title{Row-wise cosine similarities between two matrices}
\usage{
cosine_xy(
  x,
  y,
  x_labels = NULL,
  y_labels = NULL,
  id_col = "id",
  norm01 = FALSE,
  ...
)
}
\arguments{
\item{x}{A numeric matrix/data.frame (rows = items to compare).}

\item{y}{A numeric matrix/data.frame (rows = targets to compare against).}

\item{x_labels}{Optional character vector of length \code{nrow(x)} to label
rows of \code{x}. If \code{NULL}, uses \code{rownames(x)} or sequence.}

\item{y_labels}{Optional character vector of length \code{nrow(y)} to label
columns (i.e., rows of \code{y}). If \code{NULL}, uses \code{rownames(y)} or sequence.}

\item{id_col}{Name of the identifier column for \code{x} labels. Default \code{"id"}.}

\item{norm01}{Logical; if \code{TRUE}, min–max scales the similarity matrix to \link{0,1}.}

\item{...}{Additional arguments passed to \code{text2vec::sim2()}
(e.g., \code{method = "cosine"}, \code{norm = "l2"}). Defaults are those of \code{sim2()}.}
}
\value{
A \code{data.frame}: first column = \code{id_col} (x labels),
remaining columns = similarities to each row of \code{y} (named by \code{y_labels}).
}
\description{
Computes pairwise similarities between rows of \code{x} and rows of \code{y}
using \code{text2vec::sim2()}. The first column contains labels for
rows of \code{x}; columns are labeled by \code{y} row labels.
}
\details{
Non-numeric columns are dropped. If coercion produces \code{NA}s, an error is thrown.
}
\examples{
\dontrun{
library(text2vec)
set.seed(1)
x <- matrix(rnorm(9), 3, 3)
y <- matrix(rnorm(12), 4, 3)
cosine_xy(x, y,
          x_labels = paste0("x", 1:3),
          y_labels = paste0("y", 1:4),
          method = "cosine", norm = "l2")
}

}
