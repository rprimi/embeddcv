% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relabeling.R
\name{select_relabeling_solutions}
\alias{select_relabeling_solutions}
\title{Select optimal and parsimonious relabeling solutions}
\usage{
select_relabeling_solutions(
  labels_scored,
  max_constructs = 100,
  exclude = c("original_b5", "louvain")
)
}
\arguments{
\item{labels_scored}{Data frame returned by \code{score_relabeling_solutions()}.
It must contain at least \code{type}, \code{k}, \code{fallacy_sum}, \code{jangle}, \code{jingle},
and the list-column \code{label_vectors}.}

\item{max_constructs}{Integer or numeric scalar. Maximum number of clusters
allowed for the
parsimonious solution. The current implementation uses \code{k < max_constructs},
matching the Wulff & Mata (2025) relabeling workflow.}

\item{exclude}{Character vector of values from \code{labels_scored$type} to
exclude before selecting solutions. By default the original taxonomy and
Louvain sensitivity solutions are excluded.}
}
\value{
Data frame with two rows per algorithm family. The \code{selection} column
is either \code{"Optimal"} or \code{"Parsimonious"}. Panel B displays these fallacy
sums as \code{Fpars} and \code{Fopt}; the x-axis positions remain the corresponding
numbers of clusters.
}
\description{
Selects two representative solutions for each clustering family: the
globally best scoring solution and the best scoring solution under a maximum
number of clusters. These are the yellow and grey markers shown in panel B of
the relabeling figure.
}
\details{
Let \eqn{R_a} be all scored solutions for clustering family \eqn{a} after
rows in \code{exclude} have been removed. For any solution \eqn{r \in R_a}, let
\eqn{k_r} be its number of clusters and let
\eqn{F_r = J_r + A_r} be its fallacy sum, where \eqn{J_r} and \eqn{A_r} are
the jingle and jangle counts returned by \code{score_relabeling_solutions()}.

The optimal solution for each family is selected by lexicographic ordering:
first minimize fallacy sum and then, only for ties, choose the smaller number
of clusters:

\deqn{
r^{opt}_a =
\operatorname*{argmin}_{r \in R_a}(F_r, k_r).
}

The parsimonious solution applies the same ordering after restricting the
candidate set to solutions with fewer than \code{max_constructs} clusters:

\deqn{
r^{pars}_a =
\operatorname*{argmin}_{r \in R_a: k_r < K}(F_r, k_r),
}

where \eqn{K} is \code{max_constructs}. In code this is implemented by filtering
to \code{k < max_constructs}, sorting by \code{fallacy_sum} and then by \code{k}, and taking
the first row. Thus "parsimonious" means the lowest-fallacy solution under a
cluster-count ceiling, not necessarily the lowest-fallacy solution overall.
}
\references{
Wulff, D. U., & Mata, R. (2025). Semantic embeddings reveal and address
taxonomic incommensurability in psychological measurement. Nature Human
Behaviour, 9(5), 944-954.
https://doi.org/10.1038/s41562-024-02089-y
}
