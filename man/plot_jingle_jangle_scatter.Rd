% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jingle_jangle.R
\name{plot_jingle_jangle_scatter}
\alias{plot_jingle_jangle_scatter}
\title{Plot scale-label similarity scatter with jingle-jangle regions}
\usage{
plot_jingle_jangle_scatter(
  pair_table,
  thresholds = NULL,
  threshold_rule = c("wulff_mata", "quartile", "custom"),
  output_file = NULL,
  width = 7,
  height = 6,
  dpi = 300,
  point_alpha = 0.38,
  highlight_alpha = 0.65,
  point_size = 1.05,
  highlight_size = point_size,
  highlight_stroke = 0,
  region_alpha = 0,
  show_regression = TRUE,
  regression_colour = "#B23A7A",
  regression_linewidth = 1.1,
  palette = "wulff_mata",
  show_threshold_note = TRUE,
  min_constructs = 5,
  max_constructs = 100
)
}
\arguments{
\item{pair_table}{Data frame or tibble with one row per scale pair. It can be
the output of \code{jingle_jangle_pairs()} or \code{classify_jingle_jangle()}. It must
contain finite numeric columns \code{scale_cos} and \code{label_cos}; if it has no
valid \code{type} column, the function reclassifies pairs using \code{thresholds} or
\code{threshold_rule}.}

\item{thresholds}{Optional named numeric vector from
\code{jingle_jangle_thresholds()}. It must contain \code{scale_low}, \code{label_low},
\code{scale_high}, and \code{label_high}. When \code{threshold_rule = "wulff_mata"} and
this is supplied, the table is classified with these thresholds before
plotting. When \code{threshold_rule = "custom"}, this argument is required.}

\item{threshold_rule}{Character scalar. Threshold rule used for guides and
highlighted points. \code{"wulff_mata"} is the default denominator-based rule
implemented by \code{jingle_jangle_thresholds()}: \code{1 - 1 / min_constructs} and
\code{1 - 1 / max_constructs}. \code{"quartile"} uses the 25th and 75th percentiles
of the plotted pair similarities. \code{"custom"} uses the supplied
\code{thresholds}.}

\item{output_file}{Optional character path. If supplied, the plot is written
with \code{ggplot2::ggsave()}; if \code{NULL}, only the ggplot object is returned.}

\item{width, height}{Numeric scalars. Plot size in inches passed to
\code{ggplot2::ggsave()} when \code{output_file} is not \code{NULL}.}

\item{dpi}{Numeric scalar. Resolution in pixels per inch passed to
\code{ggplot2::ggsave()} when \code{output_file} is not \code{NULL}.}

\item{point_alpha}{Numeric scalar between 0 and 1. Alpha transparency for
non-fallacy points.}

\item{highlight_alpha}{Numeric scalar between 0 and 1. Alpha transparency for
highlighted fallacy points.}

\item{point_size}{Numeric scalar. Size used for the base cloud of all points.}

\item{highlight_size}{Numeric scalar. Size used for highlighted jingle and
jangle points.
Defaults to the same size as the background points so changing thresholds
does not visually redraw the scatter cloud.}

\item{highlight_stroke}{Numeric scalar. Line width for point markers when a
stroked shape is used.}

\item{region_alpha}{Numeric scalar between 0 and 1. Alpha for the shaded
fallacy regions. The default is \code{0}
so that threshold changes do not visually create disconnected quadrants;
increase it (for example \code{.04}) if you want shaded regions.}

\item{show_regression}{Logical scalar. If \code{TRUE}, add a linear regression
line.}

\item{regression_colour}{Character scalar. Color of the regression line.}

\item{regression_linewidth}{Numeric scalar. Line width of the regression
line.}

\item{palette}{Character scalar. Color palette for highlighted jingle and
jangle points.
Options are \code{"wulff_mata"} (default), \code{"cividis"}, \code{"viridis"},
\code{"magma"}, \code{"plasma"}, \code{"inferno"}, \code{"okabe_ito"}, and \code{"blue_gold"}.
The \code{"wulff_mata"} palette is a blue/gold cividis-based palette.}

\item{show_threshold_note}{Logical scalar. If \code{TRUE}, add a plot caption
reporting the quantile rule and cutoff values used in the plot.}

\item{min_constructs, max_constructs}{Numeric scalar threshold denominators
used when
\code{threshold_rule = "wulff_mata"} and \code{thresholds = NULL}. Defaults use
\code{1 - 1/5} and \code{1 - 1/100}, i.e. q80 and q99.}
}
\value{
A ggplot object. If \code{output_file} is not \code{NULL}, the same plot is
also saved to disk.
}
\description{
Draws a diagnostic scatterplot of scale similarity against label similarity.
Each point is a pair of scales. Points classified as jingle or jangle
fallacies are colored; all other points remain in the background cloud.
}
\details{
The x-axis is \code{scale_cos} and the y-axis is \code{label_cos}. The default
\code{threshold_rule = "wulff_mata"} uses the quantiles defined by
\code{jingle_jangle_thresholds()}: q80 for the low thresholds and q99 for the
high thresholds when \code{min_constructs = 5} and \code{max_constructs = 100}.
\code{threshold_rule = "quartile"} uses q25 and q75 of the plotted pair table.
\code{threshold_rule = "custom"} uses the values supplied in \code{thresholds}.

Jingle and jangle classes are computed with \code{classify_jingle_jangle()}.
The optional regression line is a linear model \eqn{label\_cos =
\beta_0 + \beta_1 scale\_cos + \epsilon} fit by \code{ggplot2::geom_smooth()}.
}
\examples{
\dontrun{
pairs <- jingle_jangle_pairs(scale_cos, label_cos, label_by_scale)
th <- jingle_jangle_thresholds(scale_cos, label_cos)

plot_jingle_jangle_scatter(pairs, thresholds = th)
plot_jingle_jangle_scatter(pairs, threshold_rule = "quartile",
                           palette = "blue_gold")
plot_jingle_jangle_scatter(
  pairs,
  threshold_rule = "custom",
  thresholds = c(scale_low = .40, label_low = .40,
                 scale_high = .80, label_high = .80),
  region_alpha = .03,
  show_threshold_note = TRUE
)
}
}
