% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relabeling.R
\name{plot_relabeling_panel_a}
\alias{plot_relabeling_panel_a}
\title{Plot panel A of the relabeling figure}
\usage{
plot_relabeling_panel_a(
  panel_a,
  fallacy_sum_text = NULL,
  jingle_fallacies_text = NULL,
  jangle_fallacies_text = NULL,
  parsimonious_solution_text = NULL,
  optimal_solution_text = NULL,
  panel_letter = "A",
  palette = "wulff_mata",
  curve_legend = TRUE,
  curve_legend_position = "topright",
  curve_legend_inset = 0.02,
  curve_legend_cex = 0.82
)
}
\arguments{
\item{panel_a}{List returned by \code{prepare_relabeling_panel_a()}. It must
contain \code{solutions_curve}, \code{original_sum}, selected \code{parsimonious} and
\code{optimal} rows, \code{xlocs}, \code{ylocs}, axis ticks, and plotting limits.}

\item{fallacy_sum_text, jingle_fallacies_text, jangle_fallacies_text}{Optional
named lists used to override direct curve-label annotations when
\code{curve_legend = FALSE}. Supported fields are \code{x}, \code{y}, \code{label}, \code{adj},
\code{cex}, \code{font}, and \code{col}, matching arguments passed to \code{graphics::text()}.
For example, \code{fallacy_sum_text = list(x = 145, y = log(500), cex = .9)}
moves the total fallacy label and changes its size.}

\item{parsimonious_solution_text, optimal_solution_text}{Optional named lists
used to override the solution annotations. Supported fields are \code{x}, \code{y},
\code{label}, \code{adj}, \code{cex}, \code{font}, and \code{col}. These lists also accept
\code{x_offset}, which is interpreted relative to the selected solution's
number of clusters. For example,
\code{parsimonious_solution_text = list(x_offset = 8, y = log(8000))} places the
annotation eight clusters to the right of the parsimonious marker.}

\item{panel_letter}{Character scalar used as the panel label, usually \code{"A"}.
Use \code{NULL} to suppress it.}

\item{palette}{Character scalar. Curve color palette. Options are
\code{"wulff_mata"} (default),
\code{"cividis"}, \code{"viridis"}, \code{"magma"}, \code{"plasma"}, \code{"inferno"},
\code{"okabe_ito"}, and \code{"blue_gold"}. The same palette controls the total,
jingle, and jangle curves.}

\item{curve_legend}{Logical scalar. If \code{TRUE} (default), the fallacy-sum,
jingle, and jangle curves are identified by a compact line legend instead
of direct labels near the curves.}

\item{curve_legend_position}{Character scalar passed to \code{graphics::legend()}.
Useful values are \code{"topright"}, \code{"topleft"}, \code{"bottomright"}, and
\code{"bottomleft"}.}

\item{curve_legend_inset}{Numeric scalar or length-two numeric vector passed
to \code{graphics::legend(inset = ...)}, used to move the legend inward from
the selected corner.}

\item{curve_legend_cex}{Numeric scalar. Text size for the panel A curve
legend.}
}
\value{
Invisibly returns \code{panel_a}.
}
\description{
Draws one relabeling curve: number of clusters on the x-axis and fallacy
counts on the log-scaled y-axis. The thick middle curve is the total fallacy
sum; the two thinner curves show jangle and jingle fallacies
separately.
}
\examples{
\dontrun{
panel_a <- prepare_relabeling_panel_a(labels_scored)
plot_relabeling_panel_a(
  panel_a,
  curve_legend = FALSE,
  fallacy_sum_text = list(x = 145, y = log(500)),
  jingle_fallacies_text = list(x = 210, y = log(350)),
  palette = "blue_gold"
)
}
}
\references{
Wulff, D. U., & Mata, R. (2025). Semantic embeddings reveal and address
taxonomic incommensurability in psychological measurement. Nature Human
Behaviour, 9(5), 944-954.
https://doi.org/10.1038/s41562-024-02089-y
}
