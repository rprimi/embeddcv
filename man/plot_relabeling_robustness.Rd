% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relabeling.R
\name{plot_relabeling_robustness}
\alias{plot_relabeling_robustness}
\title{Plot relabeling robustness curves}
\usage{
plot_relabeling_robustness(
  labels_scored,
  original_fallacy_sum = NULL,
  output_file = NULL,
  width = 10,
  height = 4,
  dpi = 300,
  palette = "wulff_mata"
)
}
\arguments{
\item{labels_scored}{Data frame returned by \code{score_relabeling_solutions()}.
It must contain columns \code{type}, \code{k}, \code{fallacy_sum}, \code{jangle}, and \code{jingle}.}

\item{original_fallacy_sum}{Optional numeric scalar for a horizontal
reference line, typically \code{score_label_solution(...)[["fallacy_sum"]]} for
the original taxonomy.}

\item{output_file}{Optional character file path for \code{ggplot2::ggsave()}. If
\code{NULL}, the plot is returned but not saved.}

\item{width, height, dpi}{Numeric scalars. Saved figure settings passed to
\code{ggplot2::ggsave()} when \code{output_file} is not \code{NULL}.}

\item{palette}{Character scalar. Color palette used for the Jingle, Jangle,
and Total curves.
Options are \code{"wulff_mata"} (default), \code{"cividis"}, \code{"viridis"},
\code{"magma"}, \code{"plasma"}, \code{"inferno"}, \code{"okabe_ito"}, and \code{"blue_gold"}.
The \code{"wulff_mata"} palette uses cividis colors close to the palette used
in Wulff & Mata (2025).}
}
\value{
A ggplot object. The y-axis uses a pseudo-log scale so small and
large fallacy counts can be inspected in the same panel.
}
\description{
Generates the relabeling robustness figure described by Wulff & Mata (2025).
The figure summarizes how the number of jingle, jangle, and total fallacies
changes as the number of clusters varies across clustering algorithms.
The layout is designed to reproduce the relabeling robustness figure style
used in the Supplementary Information of Wulff & Mata (2025).
}
\details{
The input is the scored solution table returned by
\code{score_relabeling_solutions()}. Original-taxonomy and Louvain rows are
removed by default, then scores are averaged by clustering family and number
of clusters. The plot facets algorithm families and draws three curves:
jingle fallacies, jangle fallacies, and their sum. The optional horizontal
reference line is usually the fallacy sum of the original taxonomy.
}
\references{
Wulff, D. U., & Mata, R. (2025). Semantic embeddings reveal and address
taxonomic incommensurability in psychological measurement. Nature Human
Behaviour, 9(5), 944-954.
https://doi.org/10.1038/s41562-024-02089-y
}
