% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation_figures.R
\name{plot_convergent_validity}
\alias{plot_convergent_validity}
\title{Plot convergent-validity recovery}
\usage{
plot_convergent_validity(
  convergent_tbl,
  output_file = NULL,
  width = 9,
  height = 4.8,
  dpi = 600,
  palette = "wulff_mata",
  show_legend = FALSE,
  show_construct_labels = FALSE,
  construct_label_fun = identity,
  construct_labels = NULL,
  construct_label_size = NULL,
  show_construct_n = TRUE,
  construct_label_margin = 0.015,
  construct_tick_length = 0.5,
  point_size = 10.5,
  point_stroke = 0.7,
  recovery_line = 2,
  y_limits = NULL
)
}
\arguments{
\item{convergent_tbl}{Data frame returned by \code{convergent_validity()}. It must
contain \code{model}, \code{construct}, and \code{convergent_similarity}.}

\item{output_file}{Optional character path. If supplied, the plot is written
with \code{ggplot2::ggsave()}.}

\item{width, height, dpi}{Numeric scalars passed to \code{ggplot2::ggsave()} when
\code{output_file} is not \code{NULL}.}

\item{palette}{Character scalar. Color palette for models. Options are
\code{"wulff_mata"}, \code{"cividis"}, \code{"viridis"}, \code{"magma"}, \code{"plasma"},
\code{"inferno"}, \code{"okabe_ito"}, and \code{"blue_gold"}.}

\item{show_legend}{Logical scalar. If \code{TRUE}, draws the model
legend. This is useful even for one-model displays when the plotted model
name should be explicit. The default is \code{FALSE}, matching compact
single-model panels without a legend.}

\item{show_construct_labels}{Logical scalar. If \code{TRUE}, prints construct
names below the x axis. The default is \code{FALSE}, because many constructs can
make the display crowded.}

\item{construct_label_fun}{Function used to convert construct ids into
displayed labels when \code{show_construct_labels = TRUE}.}

\item{construct_labels}{Optional character vector used to override construct
labels after \code{construct_label_fun()} is applied. If named, names must match
\code{convergent_tbl$construct}; if unnamed, length must equal the number of
plotted constructs in their display order.}

\item{construct_label_size}{Optional numeric text size for construct labels.
If \code{NULL}, the size is chosen from \code{width} and the number of constructs.}

\item{show_construct_n}{Logical scalar. If \code{TRUE} and \code{convergent_tbl}
contains \code{n_scales}, appends the number of scales to each construct label,
for example \code{"Antagonism (7)"}.}

\item{construct_label_margin}{Numeric scalar. Vertical distance between the
x-axis baseline and the rotated construct labels.}

\item{construct_tick_length}{Numeric scalar. Length, in points, of the small
x-axis ticks shown when construct labels are drawn.}

\item{point_size}{Numeric scalar. Size of the square points. The default is
intentionally large so this display has visual weight comparable to the
package's other validation panels.}

\item{point_stroke}{Numeric scalar. White outline width around square points.}

\item{recovery_line}{Numeric scalar. Horizontal reference line. The default
is 2, the recovery criterion used by Wulff & Mata (2025).}

\item{y_limits}{Optional numeric vector of length 2. If \code{NULL}, limits are
chosen from the data and \code{recovery_line}.}
}
\value{
A ggplot object. If \code{output_file} is supplied, the same plot is saved
to disk.
}
\description{
Plots construct-level convergent-validity scores returned by
\code{convergent_validity()}. Constructs are ordered by their mean score across
models, matching the visual logic of the Wulff & Mata (2025) supplementary
convergent-validity display.
}
\references{
Wulff, D. U., & Mata, R. (2025). Semantic embeddings reveal and address
taxonomic incommensurability in psychological measurement. Nature Human
Behaviour, 9(5), 944-954.
https://doi.org/10.1038/s41562-024-02089-y
}
