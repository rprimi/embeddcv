% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation_figures.R
\name{plot_semantic_validity}
\alias{plot_semantic_validity}
\title{Plot a semantic validity index by scale}
\usage{
plot_semantic_validity(
  tbl,
  index = c("fidelity", "alpha_semantic", "convergent_similarity",
    "mean_item_similarity"),
  geom = c("bar", "point"),
  recovery_line = NULL,
  show_labels = TRUE,
  label_fun = identity,
  palette = "wulff_mata",
  output_file = NULL,
  width = 9,
  height = 4.8,
  dpi = 300
)
}
\arguments{
\item{tbl}{Data frame from \code{semantic_validity()}.}

\item{index}{Character. Which column to plot. One of \code{"fidelity"}
(default), \code{"alpha_semantic"}, \code{"convergent_similarity"},
\code{"mean_item_similarity"}.}

\item{geom}{Character. \code{"bar"} (default) or \code{"point"}.}

\item{recovery_line}{Numeric or \code{NULL}. Horizontal reference line.
Defaults to \code{2} for the z-scored indices (\code{fidelity},
\code{convergent_similarity}) and \code{NULL} otherwise.}

\item{show_labels}{Logical. Show scale labels on the x axis (default
\code{TRUE}).}

\item{label_fun}{Function mapping scale ids to display labels.}

\item{palette}{Character. Color palette (see \code{plot_divergent_validity}).}

\item{output_file, width, height, dpi}{Optional save path and size for
\code{ggplot2::ggsave()}.}
}
\value{
A ggplot object (also saved when \code{output_file} is supplied).
}
\description{
Bar (or point) plot of one per-scale index from \code{\link{semantic_validity}},
ordered from highest to lowest. A dashed reference line marks the recovery
criterion for z-scored indices.
}
\seealso{
\code{\link{semantic_validity}}
}
