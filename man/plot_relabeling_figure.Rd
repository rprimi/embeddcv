% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relabeling.R
\name{plot_relabeling_figure}
\alias{plot_relabeling_figure}
\title{Plot the three-panel Wulff & Mata-style relabeling figure}
\usage{
plot_relabeling_figure(
  labels_scored = NULL,
  scale_label_cross = NULL,
  label_by_scale = NULL,
  relabeling_panels = NULL,
  output_file = NULL,
  max_constructs = 100,
  solution = c("parsimonious", "optimal"),
  solution_type = NULL,
  width = 12,
  height = 15,
  res = 600,
  labels = NULL,
  instrument = NULL,
  fallacy_sum_text = NULL,
  jingle_fallacies_text = NULL,
  jangle_fallacies_text = NULL,
  parsimonious_solution_text = NULL,
  optimal_solution_text = NULL,
  curve_legend = TRUE,
  curve_legend_position = "topright",
  curve_legend_inset = 0.02,
  curve_legend_cex = 0.82,
  palette = "wulff_mata",
  show_model_note = FALSE,
  model_note = NULL,
  model_note_cex = 0.72
)
}
\arguments{
\item{labels_scored}{Data frame returned by \code{score_relabeling_solutions()}.
Required when \code{relabeling_panels = NULL}; ignored when \code{relabeling_panels}
is supplied.}

\item{scale_label_cross}{Numeric scale-by-label similarity matrix. Required
when \code{relabeling_panels = NULL}. Rows are scale ids; columns are candidate
label ids.}

\item{label_by_scale}{Named character vector mapping each scale id to its
original label id. Required when \code{relabeling_panels = NULL}. Names must be
scale ids and values must be column names in \code{scale_label_cross}.}

\item{relabeling_panels}{Optional list returned by
\code{prepare_relabeling_panels()}. If supplied, the three panels are drawn from
this object and the raw preparation inputs are ignored.}

\item{output_file}{Optional character PNG path. If \code{NULL}, the plot is drawn
on the active graphics device and no file is written.}

\item{max_constructs}{Integer or numeric scalar. Maximum cluster count for
parsimonious solutions when panel data must be prepared internally.}

\item{solution}{Character scalar, either \code{"parsimonious"} or \code{"optimal"}.
Used only when \code{relabeling_panels = NULL}.}

\item{solution_type}{Optional character scalar naming a clustering family to
select within, such as \code{"hclust_ward"}, \code{"kmeans"}, or \code{"mclust"}. If
\code{NULL}, selection is global across candidate families. Used only when
\code{relabeling_panels = NULL}.}

\item{width, height}{Numeric scalars. PNG size in inches when \code{output_file} is
supplied.}

\item{res}{Numeric scalar. PNG resolution in pixels per inch when
\code{output_file} is supplied.}

\item{labels}{Optional named character vector mapping scale/label ids to
display text. Used only when \code{relabeling_panels = NULL}. If \code{NULL}, ids are
shown as-is.}

\item{instrument}{Optional named character vector mapping scale ids to an
instrument name, appended as \code{"label (instrument)"}. Used only when
\code{relabeling_panels = NULL}.}

\item{fallacy_sum_text, jingle_fallacies_text, jangle_fallacies_text}{Optional
named lists passed to \code{plot_relabeling_panel_a()}. Each list can contain
\code{x}, \code{y}, \code{label}, \code{adj}, \code{cex}, \code{font}, and \code{col}.}

\item{parsimonious_solution_text, optimal_solution_text}{Optional named lists
passed to \code{plot_relabeling_panel_a()}. These lists can also use \code{x_offset}
to place text relative to the selected solution marker.}

\item{curve_legend, curve_legend_position, curve_legend_inset, curve_legend_cex}{Curve legend settings passed to \code{plot_relabeling_panel_a()}.}

\item{palette}{Character scalar. Palette used across the panels. Options are
\code{"wulff_mata"} (default), \code{"cividis"}, \code{"viridis"}, \code{"magma"},
\code{"plasma"}, \code{"inferno"}, \code{"okabe_ito"}, and \code{"blue_gold"}.
This single argument is passed to panels A, B, and C.}

\item{show_model_note}{Logical scalar. If \code{TRUE}, adds a small footnote below
the figure identifying the model/solution used in panel A and panel C. The
default is \code{FALSE} so package users opt in explicitly.}

\item{model_note}{Optional character scalar. Custom note to draw when
\code{show_model_note = TRUE}. If \code{NULL}, the note is generated from
\code{relabeling_panels}.}

\item{model_note_cex}{Numeric scalar. Character expansion for the model note.}
}
\value{
Invisibly returns \code{output_file}.
}
\description{
Generates the relabeling figure in the style of Wulff & Mata (2025). The
figure summarizes how semantic relabeling can reduce taxonomic
incommensurability. By default, panel A and panel C are keyed to the same
global parsimonious solution. Panel A shows how jingle, jangle, and total
fallacy counts change across that solution's clustering family. Panel B
compares parsimonious and optimal solutions across clustering algorithms.
Panel C draws the circular relabeling assignment map for the selected
solution; solid links denote assignments among a scale's top five candidate
labels and dashed/dotted links denote lower-ranked assignments.
This function is a high-level wrapper around
\code{prepare_relabeling_panels()}, \code{plot_relabeling_panel_a()},
\code{plot_relabeling_panel_b()}, and \code{plot_relabeling_panel_c()}.
}
\details{
If \code{relabeling_panels} is not supplied, the function prepares all panel data
internally from \code{labels_scored}, \code{scale_label_cross}, and \code{label_by_scale}.
Supplying a precomputed \code{relabeling_panels} object is useful when the data are
expensive to prepare or when you want to inspect the selected solution before
plotting.

With the default \code{solution = "parsimonious"} and \code{solution_type = NULL}, the
selected solution minimizes \code{fallacy_sum} among candidate clustering
solutions with \eqn{k <} \code{max_constructs}, breaking ties by smaller \code{k}.
With \code{solution = "optimal"}, the selected solution minimizes \code{fallacy_sum}
without the \eqn{k <} \code{max_constructs} restriction. If \code{solution_type} is
supplied, selection is restricted to that clustering family instead of being
global. Panel A plots the full trajectory for the same clustering family as
this selected solution; panel C draws the selected solution as the circular
scale-to-label map.

The circular label layout and color-mixing helpers used for panel C are local
implementations adapted from plotting utilities in the \code{memnet} package
(Wulff, 2019): \code{embeddcv_circle_raw()} follows the geometry of its internal
\code{circle_raw()} helper, and \code{mix_col()} follows the RGB interpolation behavior
of \code{memnet::cmix()}.

The PNG device is opened with \code{grDevices::png(width = width, height = height, res = res, units = "in")}. Thus \code{width} and \code{height} are in inches, and
\code{res} is pixels per inch. The final pixel size is approximately
\code{width * res} by \code{height * res}.
}
\examples{
\dontrun{
plot_relabeling_figure(
  labels_scored = labels_scored,
  scale_label_cross = scale_label_cross,
  label_by_scale = label_by_scale,
  output_file = "04_relabeling.png",
  palette = "wulff_mata",
  fallacy_sum_text = list(x = 145, y = log(500)),
  jingle_fallacies_text = list(x = 210, y = log(300))
)
}
}
\references{
Wulff, D. U., & Mata, R. (2025). Semantic embeddings reveal and address
taxonomic incommensurability in psychological measurement. Nature Human
Behaviour, 9(5), 944-954.
https://doi.org/10.1038/s41562-024-02089-y

Wulff, D. U. (2019). memnet: Network Tools for Memory Research. R package.
https://www.rdocumentation.org/packages/memnet/versions/0.1.0
}
