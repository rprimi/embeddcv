% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relabeling.R
\name{score_clustering_solution}
\alias{score_clustering_solution}
\title{Relabel and score one clustering solution}
\usage{
score_clustering_solution(
  clustering_solution,
  scale_label_cross,
  scale_cos,
  label_cos,
  thresholds
)
}
\arguments{
\item{clustering_solution}{Named cluster membership vector, usually one
element of the list returned by \code{make_relabeling_clustering_solutions()}.
Names are scale ids; values identify cluster membership.}

\item{scale_label_cross}{Numeric scale-by-label similarity matrix used for
maximum-weight relabeling. Rows must include the scale ids in
\code{names(clustering_solution)} and columns must be candidate label ids.}

\item{scale_cos}{Numeric square scale-scale similarity matrix used to score
jingle and jangle fallacies. Row and column names must include the scale
ids in \code{names(clustering_solution)}.}

\item{label_cos}{Numeric square label-label similarity matrix used to score
assigned labels. Row and column names must include the candidate labels in
\code{colnames(scale_label_cross)}.}

\item{thresholds}{Named numeric vector from \code{jingle_jangle_thresholds()} with
\code{scale_low}, \code{label_low}, \code{scale_high}, and \code{label_high}.}
}
\value{
A list with two components:
\describe{
\item{labels}{Named character vector returned by \code{relabel_solution()}.}
\item{scores}{Named integer vector returned by \code{score_label_solution()}.}
}
}
\description{
Convenience wrapper that first calls \code{relabel_solution()} and then evaluates
the resulting label assignment with \code{score_label_solution()}. This implements
the candidate-taxonomy scoring step used by Wulff & Mata (2025): a cluster
solution is relabeled by maximum-weight bipartite matching between clusters
and labels, and the resulting label assignment is scored for jingle
fallacies (similar labels assigned to dissimilar scales) and jangle
fallacies (dissimilar labels assigned to similar scales). Use this when you
already have one candidate taxonomy and want both its matched labels and its
jingle/jangle counts.
}
\references{
Wulff, D. U., & Mata, R. (2025). Semantic embeddings reveal and address
taxonomic incommensurability in psychological measurement. Nature Human
Behaviour, 9(5), 944-954.
https://doi.org/10.1038/s41562-024-02089-y
}
