% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jingle_jangle.R
\name{plot_jingle_jangle_cutoffs}
\alias{plot_jingle_jangle_cutoffs}
\title{Plot jingle-jangle cutoff sensitivity grids}
\usage{
plot_jingle_jangle_cutoffs(
  threshold_grid,
  panels = c("jingle", "jangle", "scale_low", "scale_high", "label_low", "label_high"),
  panel_labels = NULL,
  output_file = NULL,
  width = 10,
  height = 6.5,
  res = 300,
  palette = "viridis",
  n_colours = 20,
  text_col = "white",
  border_col = NA,
  digits = 2,
  text_cex = 0.6,
  axis_cex = 0.8,
  title_cex = 1,
  label_cells = TRUE,
  mar = c(4, 4.4, 2.8, 0.8)
)
}
\arguments{
\item{threshold_grid}{Data frame returned by
\code{jingle_jangle_threshold_grid()}. It must contain \code{lows}, \code{highs}, \code{x},
\code{y}, \code{jingle}, \code{jangle}, \code{scale_low}, \code{scale_high}, \code{label_low}, and
\code{label_high}.}

\item{panels}{Character vector naming the panels to draw. Available panels
are \code{"jingle"}, \code{"jangle"}, \code{"scale_low"}, \code{"scale_high"},
\code{"label_low"}, and \code{"label_high"}.}

\item{panel_labels}{Optional named character vector used to relabel panel
titles. Names must match values in \code{panels}.}

\item{output_file}{Optional path. If supplied, a file is opened and the plot
is written to disk. Files ending in \code{.pdf} use \code{grDevices::pdf()}; all
other extensions use \code{grDevices::png()}.}

\item{width, height}{Numeric scalars. Device size in inches.}

\item{res}{Numeric scalar. PNG resolution in pixels per inch.}

\item{palette}{Character scalar. Tile palette. Options are \code{"viridis"}
(default), \code{"wulff_mata"}, \code{"cividis"}, \code{"magma"}, \code{"plasma"},
\code{"inferno"}, \code{"okabe_ito"}, and \code{"blue_gold"}. The default uses the same
viridis endpoint as the cutoff-sensitivity display in Wulff & Mata (2025).}

\item{n_colours}{Integer scalar. Number of discrete colors used to map each
panel's values. The default \code{20} follows the Wulff & Mata (2025)
cutoff-sensitivity display.}

\item{text_col}{Character scalar. Cell-label color.}

\item{border_col}{Character scalar or \code{NA}. Tile border color.}

\item{digits}{Integer scalar. Number of decimal places for cutoff-value
panels. Count panels are printed as integers.}

\item{text_cex, axis_cex, title_cex}{Numeric scalars passed to base graphics
text drawing.}

\item{label_cells}{Logical scalar. If \code{TRUE}, writes each cell value inside
the tile.}

\item{mar}{Numeric vector of length four passed to \code{graphics::par(mar = )}
for each panel.}
}
\value{
Invisibly returns \code{threshold_grid}.
}
\description{
Draws the cutoff-sensitivity display for a table returned by
\code{jingle_jangle_threshold_grid()}. Each panel is a tile grid whose rows and
columns are the high- and low-threshold denominators used to classify
jingle and jangle fallacies. The default panels show how the number of
jingle fallacies, number of jangle fallacies, and the corresponding
similarity cutoffs change as the assumed minimum and maximum number of
constructs vary.
}
\details{
Let \eqn{a} be a value in \code{lows} and \eqn{b} be a value in \code{highs}.
\code{jingle_jangle_threshold_grid()} converts those denominator rules into
quantiles:
\deqn{c_{\mathrm{low}} = Q(1 - 1/a)}
\deqn{c_{\mathrm{high}} = Q(1 - 1/b)}

The low and high cutoffs are computed separately for the scale-similarity
distribution and the label-similarity distribution. A scale pair is counted
as a jingle fallacy when label similarity is at or above its high cutoff and
scale similarity is at or below its low cutoff. A scale pair is counted as a
jangle fallacy when scale similarity is at or above its high cutoff and label
similarity is at or below its low cutoff. This is the same cutoff-sensitivity
logic used by Wulff & Mata (2025) to inspect whether conclusions depend on
the chosen construct-count assumptions.
}
\references{
Wulff, D. U., & Mata, R. (2025). Semantic embeddings reveal and address
taxonomic incommensurability in psychological measurement. Nature Human
Behaviour, 9(5), 944-954.
https://doi.org/10.1038/s41562-024-02089-y
}
