% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/semantic_helpers.R
\name{get_scale_embeddings}
\alias{get_scale_embeddings}
\title{Average item embeddings by scale}
\usage{
get_scale_embeddings(emb, scale_id)
}
\arguments{
\item{emb}{Numeric matrix or data frame of item embeddings. Rows are items
and columns are embedding dimensions. This can be the output of
\code{get_embeddings()} after converting to a numeric matrix.}

\item{scale_id}{Character or factor vector assigning each item row to a
scale. Must have length \code{nrow(emb)}. Unique values become row names in the
returned scale embedding matrix.}
}
\value{
Numeric matrix with one row per unique scale id and one column per
embedding dimension.
}
\description{
Aggregates item-level embeddings into scale-level embeddings by taking the
column-wise mean of all item vectors that belong to the same scale.
}
\details{
For scale \eqn{s} with item set \eqn{I_s}, the scale embedding is

\deqn{\bar{x}_s = \frac{1}{|I_s|}\sum_{i \in I_s} x_i.}

Missing values are removed dimension-wise with \code{na.rm = TRUE}.
}
