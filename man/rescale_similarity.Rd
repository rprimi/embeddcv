% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/semantic_helpers.R
\name{rescale_similarity}
\alias{rescale_similarity}
\title{Rescale similarities to a target interval}
\usage{
rescale_similarity(x, to = c(0, 1), from = NULL)
}
\arguments{
\item{x}{Numeric vector, matrix, or array to rescale. Existing dimensions,
names, and dimnames are preserved in the returned object.}

\item{to}{Numeric vector of length 2 with the target range, for example
\code{c(0, 1)}. The first value is the new minimum and the second value is the
new maximum.}

\item{from}{Optional numeric vector of length 2 with the input range. When
\code{NULL}, the finite range of \code{x} is used. Supply this when multiple objects
must be placed on the same scale.}
}
\value{
Object with the same dimensions and dimnames as \code{x}.
}
\description{
Rescales a numeric vector, matrix, or array linearly to a target interval.
This is useful for reproducing the relabeling workflow from Wulff & Mata
(2025), where scale-scale cosine similarities are min-max rescaled before
clustering.
}
\details{
For input value \eqn{x}, input range \eqn{[a,b]}, and target range
\eqn{[c,d]}, the transformed value is

\deqn{x' = \frac{x-a}{b-a}(d-c)+c.}

When \code{from = NULL}, \eqn{a} and \eqn{b} are the finite minimum and maximum of
\code{x}. Missing and infinite values are not used to determine the range, but are
preserved in the output.
}
\references{
Wulff, D. U., & Mata, R. (2025). Semantic embeddings reveal and address
taxonomic incommensurability in psychological measurement. Nature Human
Behaviour, 9(5), 944-954.
https://doi.org/10.1038/s41562-024-02089-y
}
